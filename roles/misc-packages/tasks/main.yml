---
# tasks file for misc-packages

- name: Install misc packages
  package: 
    name: "{{ packages }}"
    state: present
  become: yes
  vars: 
    packages:
      - diceware
      - zsh
      - nano
      - rsync
      - zip
  tags: 
    - packages

- name: Conduct Safe Update
  apt:
    upgrade: safe
  become: yes
  tags:
    - never
    - update
  
